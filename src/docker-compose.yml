version: "3.9"

services:
  otp:
    image: opentripplanner/opentripplanner:2.8.1
    working_dir: /var/opentripplanner
    ports:
      - "8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-Xmx20g"
    volumes:
      - ./otp-data:/var/opentripplanner
    command: ["--load", "--serve"]
    restart: unless-stopped

  opensearch:
    image: opensearchproject/opensearch:latest
    environment:
      - discovery.type=single-node
      - DISABLE_SECURITY_PLUGIN=true
      - DISABLE_INSTALL_DEMO_CONFIG=true
      - bootstrap.memory_lock=true
      - bootstrap.system_call_filter=false
      - OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    ports:
      - "9200:9200"
      - "9600:9600"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200/_cluster/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  opensearch-dashboards:
    image: opensearchproject/opensearch-dashboards:latest
    container_name: opensearch-dashboard
    restart: unless-stopped
    environment:
      - 'OPENSEARCH_HOSTS=["http://opensearch:9200"]'
      - DISABLE_SECURITY_DASHBOARDS_PLUGIN=true
    ports:
      - 5601:5601
    depends_on:
      - opensearch

  backend:
    build:
      context: .
      dockerfile: Backend/Dockerfile
    env_file:
      - .env
    environment:
      - OPENSEARCH_HOST=opensearch
      - OPENSEARCH_PORT=9200
      - OTP_URL=http://otp:8080/otp/routers/default/index/graphql
    depends_on:
      otp:
        condition: service_started
      opensearch:
        condition: service_healthy
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
        context: ./Frontend
        dockerfile: Dockerfile
    depends_on:
        - backend
    ports:
        - "5173:5173"
    restart: unless-stopped

  # Ingestion toolbox container (does NOT auto-run ingestor_v2).
  # Use:
  #   docker compose --profile ingest run --rm ingester python ingestor_v2.py
  ingester:
    profiles: ["ingest"]
    build:
      context: ./Backend/Ingester
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - OPENSEARCH_HOST=opensearch
      - OPENSEARCH_PORT=9200
      - OTP_URL=http://otp:8080/otp/routers/default/index/graphql
      - BAYERNCLOUD_DATA_DIR=/data/bayerncloud
    volumes:
      - bc-data:/data/bayerncloud
    depends_on:
      - opensearch
    command: ["sleep", "infinity"]
    restart: "no"

  # BayernCloud fetcher (optional): downloads BayernCloud JSON into the shared bc-data volume.
  # Use:
  #   docker compose --profile ingest run --rm bayerncloud-fetch
  bayerncloud-fetch:
    profiles: ["ingest"]
    build:
      context: ./Backend/Ingester
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - BAYERNCLOUD_DATA_DIR=/data/bayerncloud
    volumes:
      - bc-data:/data/bayerncloud
    command: ["python", "bayerncloud_fetch.py"]
    restart: "no"

volumes:
  opensearch-data:
    external: true
    name: kira-project_os_data
  bc-data:
